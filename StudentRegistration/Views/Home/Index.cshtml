@* @model IEnumerable<StudentViewModel>

@{
    ViewData["Title"] = "All Students";
}

<div class="container-fluid px-4 mx-2">
    <div class="row">
        <div class="col-md-12">
            <h3 class="text-center text-decoration-underline">All Employees List</h3>
        </div>
    </div>
    <div class="row mx-auto">
        <div class="col-md-12">
            <a class="btn btn-primary" asp-controller="Home" asp-action="Add">
                <i class="fa-solid fa-square-plus"></i> New Employee
            </a>
        </div>
    </div>

    <div class="row">
        <div class="col-md-12 p-4">
            <table class="table table-bordered px-2">
                <thead class="table-light">
                    <tr>
                        <th scope="col">#</th>
                        <th scope="col">FirstName</th>
                        <th scope="col">LastName</th>
                        <th scope="col">DateOfBirth</th>
                        <th scope="col">Gender</th>
                        <th scope="col">Email</th>
                        <th scope="col">PhoneNumber</th>
                        <th scope="col">Address</th>
                        <th></th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var student in Model)
                    {
                        <tr>
                            <th scope="row">@student.StudentId</th>
                            <td>@student.FirstName</td>
                            <td>@student.LastName</td>
                            <td>@student.DateOfBirth</td>
                            <td>@student.Gender</td>
                            <td>@student.Email</td>
                            <td>@student.PhoneNumber</td>
                            <td>@student.Address</td>
                            <td><a asp-controller="Home" asp-action="Edit" asp-route-id="@student.StudentId">
                                    <i class="fa-solid fa-pen-to-square"></i>
                                </a>
                            </td>
                            <td><a asp-action="Delete" asp-controller="Home" asp-route-id="@student.StudentId" onClick="return confirm('Are you sure you want to delete this employee?')">
                                    <i class="fa-solid fa-trash" style=" color: red;"></i>
                                </a>
                            </td>
                        </tr>
                    }

                </tbody>

            </table>
        </div>
    </div>
   
</div>

 *@
<div class="container-fluid px-4 mx-2">
    <div class="row mt-auto">
        <div class="col-md-12">
            <h1 class="text-center" style="color: purple;">Welcome To</h1>
            <h2 class="text-center" style="color: purple;">Cash Flow Projection System</h2>
        </div>
    </div>
</div>